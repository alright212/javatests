#include <stdio.h> 
#include <stdlib.h> 
#include <math.h> 

int main() { 
int a, b; 
printf("Enter two numbers:"); 
scanf("%d, %d", &a, &b); 
if (a == b) 
{ return 0; } 
else { int Lcm = a * b; 
printf("The least common multiple of %d and %d is %d", a, b, Lcm);
} 
return 0; 
}
